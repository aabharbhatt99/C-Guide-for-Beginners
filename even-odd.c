//make a function to find,separate and print even and odd number separately.
//hint
//take an array and divide it into two arrays of even number's array and odd number's array
//then print those two array

#include<stdio.h>
#include<stdlib.h>

int main()
{
   int i, arr[10], even-arr[10], odd-arr[10];
   printf("give values of the array");
   for (i=0;i<10;i++)
   {
     scanf("%d", &arr[i]);
   }
   
   //write your code here
  
  return 0;
}
